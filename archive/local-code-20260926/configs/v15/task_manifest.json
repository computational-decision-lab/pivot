{
  "schema_version": "pivot-v15-task-manifest-2",
  "sealed": true,
  "planes": {
    "proxy": [
      {
        "task_id": "proxy-bug-001",
        "family": "bug_fixing",
        "files": {
          "src/stats.py": "def mean(values):\n    return sum(values) / (len(values) + 1)\n",
          "tests/test_stats.py": "import unittest\nfrom src.stats import mean\n\nclass MeanTest(unittest.TestCase):\n    def test_mean_of_three_values(self):\n        self.assertEqual(mean([2, 4, 6]), 4)\n\nif __name__ == '__main__':\n    unittest.main()\n",
          "README.md": "Fix the statistics helper without changing its public function name.\n"
        },
        "metadata": {
          "instruction": "Fix the mean helper so the registered test passes. Keep the public API unchanged and run the test command.",
          "test_command": "python -m unittest discover -s tests -v"
        }
      },
      {
        "task_id": "proxy-bug-002",
        "family": "bug_fixing",
        "files": {
          "src/ini.py": "def parse(text):\n    result = {}\n    for line in text.splitlines():\n        if line and not line.startswith('#'):\n            key, value = line.split('=', 1)\n            result[key] = value\n    return result\n",
          "tests/test_ini.py": "import unittest\nfrom src.ini import parse\n\nclass IniTest(unittest.TestCase):\n    def test_spaces_and_comments(self):\n        self.assertEqual(parse('# note\\n enabled = true \\n'), {'enabled': 'true'})\n\nif __name__ == '__main__':\n    unittest.main()\n",
          "README.md": "The parser accepts simple key/value configuration files.\n"
        },
        "metadata": {
          "instruction": "Repair the configuration parser for whitespace and comment lines. Preserve the parse function API and run the tests.",
          "test_command": "python -m unittest discover -s tests -v"
        }
      },
      {
        "task_id": "proxy-tools-001",
        "family": "tool_context",
        "files": {
          "src/cache.py": "class Cache:\n    def __init__(self):\n        self.values = {}\n\n    def get_or_compute(self, key, compute):\n        if key in self.values and self.values[key]:\n            return self.values[key]\n        value = compute()\n        self.values[key] = value\n        return value\n",
          "tests/test_cache.py": "import unittest\nfrom src.cache import Cache\n\nclass CacheTest(unittest.TestCase):\n    def test_falsey_values_are_cached(self):\n        calls = []\n        cache = Cache()\n        compute = lambda: calls.append(1) or 0\n        self.assertEqual(cache.get_or_compute('x', compute), 0)\n        self.assertEqual(cache.get_or_compute('x', compute), 0)\n        self.assertEqual(len(calls), 1)\n\nif __name__ == '__main__':\n    unittest.main()\n",
          "docs/workflow.md": "Navigate from the cache implementation to its tests before editing.\n"
        },
        "metadata": {
          "instruction": "Fix the cache so a computed falsey value is reused. Inspect both source and tests, then run the registered test command.",
          "test_command": "python -m unittest discover -s tests -v"
        }
      },
      {
        "task_id": "proxy-tools-002",
        "family": "tool_context",
        "files": {
          "src/report.py": "def render(rows):\n    return '\\n'.join(' | '.join(row) for row in rows)\n",
          "tests/test_report.py": "import unittest\nfrom src.report import render\n\nclass ReportTest(unittest.TestCase):\n    def test_rows_are_rendered_as_csv(self):\n        self.assertEqual(render([['name', 'score'], ['Ada', '10']]), 'name,score\\nAda,10')\n\nif __name__ == '__main__':\n    unittest.main()\n",
          "docs/format.md": "The report format is comma-separated and keeps row order.\n"
        },
        "metadata": {
          "instruction": "Correct the report renderer to produce comma-separated rows in input order. Keep the render API and run tests.",
          "test_command": "python -m unittest discover -s tests -v"
        }
      }
    ],
    "gate": [
      {
        "task_id": "gate-bug-001",
        "family": "bug_fixing",
        "files": {
          "src/stats.py": "def mean(values):\n    return sum(values) / (len(values) + 2)\n",
          "tests/test_stats.py": "import unittest\nfrom src.stats import mean\n\nclass MeanTest(unittest.TestCase):\n    def test_mean_of_four_values(self):\n        self.assertEqual(mean([1, 3, 5, 7]), 4)\n\nif __name__ == '__main__':\n    unittest.main()\n",
          "README.md": "Repair the statistics helper while keeping its public API.\n"
        },
        "metadata": {
          "instruction": "Fix the mean helper so the registered test passes. Keep the public API unchanged and run the test command.",
          "test_command": "python -m unittest discover -s tests -v"
        }
      },
      {
        "task_id": "gate-bug-002",
        "family": "bug_fixing",
        "files": {
          "src/ini.py": "def parse(text):\n    result = {}\n    for line in text.splitlines():\n        if line and not line.startswith(';'):\n            key, value = line.split('=', 1)\n            result[key] = value\n    return result\n",
          "tests/test_ini.py": "import unittest\nfrom src.ini import parse\n\nclass IniTest(unittest.TestCase):\n    def test_spaces_and_semicolon_comments(self):\n        self.assertEqual(parse('; note\\n retries = 3 \\n'), {'retries': '3'})\n\nif __name__ == '__main__':\n    unittest.main()\n",
          "README.md": "The parser accepts simple key/value configuration files.\n"
        },
        "metadata": {
          "instruction": "Repair the configuration parser for whitespace and semicolon comment lines. Preserve the parse API and run the tests.",
          "test_command": "python -m unittest discover -s tests -v"
        }
      },
      {
        "task_id": "gate-tools-001",
        "family": "tool_context",
        "files": {
          "src/cache.py": "class Cache:\n    def __init__(self):\n        self.values = {}\n\n    def get_or_compute(self, key, compute):\n        if key in self.values and self.values[key] is not None:\n            return self.values[key]\n        value = compute()\n        self.values[key] = value\n        return value\n",
          "tests/test_cache.py": "import unittest\nfrom src.cache import Cache\n\nclass CacheTest(unittest.TestCase):\n    def test_none_values_are_cached(self):\n        calls = []\n        cache = Cache()\n        compute = lambda: calls.append(1) or None\n        self.assertIsNone(cache.get_or_compute('x', compute))\n        self.assertIsNone(cache.get_or_compute('x', compute))\n        self.assertEqual(len(calls), 1)\n\nif __name__ == '__main__':\n    unittest.main()\n",
          "docs/workflow.md": "Read the cache implementation and its tests before editing.\n"
        },
        "metadata": {
          "instruction": "Fix the cache so a computed None value is reused. Inspect source and tests, then run the registered command.",
          "test_command": "python -m unittest discover -s tests -v"
        }
      },
      {
        "task_id": "gate-tools-002",
        "family": "tool_context",
        "files": {
          "src/report.py": "def render(rows):\n    return '\\n'.join(' | '.join(row) for row in reversed(rows))\n",
          "tests/test_report.py": "import unittest\nfrom src.report import render\n\nclass ReportTest(unittest.TestCase):\n    def test_rows_keep_order_and_use_commas(self):\n        self.assertEqual(render([['id', 'value'], ['a', '1']]), 'id,value\\na,1')\n\nif __name__ == '__main__':\n    unittest.main()\n",
          "docs/format.md": "Reports are comma-separated and preserve the input order.\n"
        },
        "metadata": {
          "instruction": "Correct the report renderer to produce comma-separated rows in input order. Keep the render API and run tests.",
          "test_command": "python -m unittest discover -s tests -v"
        }
      }
    ],
    "assessment": [
      {
        "task_id": "assessment-bug-001",
        "family": "bug_fixing",
        "files": {
          "src/stats.py": "def mean(values):\n    return sum(values) / (len(values) + 3)\n",
          "tests/test_stats.py": "import unittest\nfrom src.stats import mean\n\nclass MeanTest(unittest.TestCase):\n    def test_mean_of_two_values(self):\n        self.assertEqual(mean([3, 5]), 4)\n\nif __name__ == '__main__':\n    unittest.main()\n",
          "README.md": "Repair the statistics helper while keeping its public API.\n"
        },
        "metadata": {
          "instruction": "Fix the mean helper so the registered test passes. Keep the public API unchanged and run the test command.",
          "test_command": "python -m unittest discover -s tests -v"
        }
      },
      {
        "task_id": "assessment-bug-002",
        "family": "bug_fixing",
        "files": {
          "src/ini.py": "def parse(text):\n    result = {}\n    for line in text.splitlines():\n        if line and not line.startswith('#'):\n            key, value = line.split('=', 1)\n            result[key] = value\n    return result\n",
          "tests/test_ini.py": "import unittest\nfrom src.ini import parse\n\nclass IniTest(unittest.TestCase):\n    def test_whitespace_is_normalized(self):\n        self.assertEqual(parse(' timeout = 5 \\n'), {'timeout': '5'})\n\nif __name__ == '__main__':\n    unittest.main()\n",
          "README.md": "The parser accepts simple key/value configuration files.\n"
        },
        "metadata": {
          "instruction": "Repair the configuration parser so surrounding whitespace is normalized. Preserve the parse API and run tests.",
          "test_command": "python -m unittest discover -s tests -v"
        }
      },
      {
        "task_id": "assessment-tools-001",
        "family": "tool_context",
        "files": {
          "src/cache.py": "class Cache:\n    def __init__(self):\n        self.values = {}\n\n    def get_or_compute(self, key, compute):\n        if key in self.values and self.values[key]:\n            return self.values[key]\n        value = compute()\n        self.values[key] = value\n        return value\n",
          "tests/test_cache.py": "import unittest\nfrom src.cache import Cache\n\nclass CacheTest(unittest.TestCase):\n    def test_empty_string_is_cached(self):\n        calls = []\n        cache = Cache()\n        compute = lambda: calls.append(1) or ''\n        self.assertEqual(cache.get_or_compute('x', compute), '')\n        self.assertEqual(cache.get_or_compute('x', compute), '')\n        self.assertEqual(len(calls), 1)\n\nif __name__ == '__main__':\n    unittest.main()\n",
          "docs/workflow.md": "Read source and tests before editing the cache.\n"
        },
        "metadata": {
          "instruction": "Fix the cache so a computed empty string is reused. Inspect source and tests, then run the registered command.",
          "test_command": "python -m unittest discover -s tests -v"
        }
      },
      {
        "task_id": "assessment-tools-002",
        "family": "tool_context",
        "files": {
          "src/report.py": "def render(rows):\n    return '\\n'.join(' | '.join(row) for row in rows)\n",
          "tests/test_report.py": "import unittest\nfrom src.report import render\n\nclass ReportTest(unittest.TestCase):\n    def test_single_row_is_csv(self):\n        self.assertEqual(render([['status', 'ok']]), 'status,ok')\n\nif __name__ == '__main__':\n    unittest.main()\n",
          "docs/format.md": "Reports use commas between fields.\n"
        },
        "metadata": {
          "instruction": "Correct the report renderer to produce comma-separated rows. Keep the render API and run tests.",
          "test_command": "python -m unittest discover -s tests -v"
        }
      }
    ]
  },
  "outcome_visibility": {
    "operator": ["proxy"],
    "promotion": ["proxy", "gate"],
    "pivot": ["proxy", "gate"],
    "terminal_assessor": ["assessment"]
  }
}
