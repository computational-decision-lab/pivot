\documentclass{article}
\usepackage{iclr2027_conference,times}

\usepackage{amsmath,amssymb,amsthm}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{microtype}
\usepackage{xcolor}
\usepackage{url}
\usepackage{algorithm}
\usepackage{algorithmic}
\usepackage{flafter}

\graphicspath{{figures/}}

\setlength{\textfloatsep}{7pt plus 1pt minus 2pt}
\setlength{\floatsep}{6pt plus 1pt minus 2pt}
\setlength{\intextsep}{6pt plus 1pt minus 2pt}
\newcommand{\E}{\mathbb{E}}
\newcommand{\R}{\mathbb{R}}
\newcommand{\M}{\mathcal{M}}
\newcommand{\J}{\mathcal{J}}
\newcommand{\dproxy}{\Delta_V}
\newcommand{\dtrue}{\Delta_*}
\newcommand{\pii}{\pi}
\newcommand{\pip}{\pi'}
\newtheorem{proposition}{Proposition}
\newtheorem{lemma}{Lemma}
\input{results_macros.tex}
\input{revision_results.tex}
\input{highway_results.tex}

\title{When Better Gets Worse: Improvement Fidelity for Self-Improving Agents in Adaptive Worlds}
\author{}
\date{}

\begin{document}
\maketitle

\begin{abstract}
Self-improving agents often select policy updates using cheap verifiers, but deploying an update can change the environment and the behavior of other agents. We formalize this mismatch through \emph{Improvement Fidelity}, which asks whether proxy improvement preserves the sign and ordering of deployment improvement over the updates an improvement process actually proposes. Our analysis shows that strong global policy evaluation need not imply reliable update evaluation: operator shift and deployment response can create local fidelity errors, while only errors large relative to candidate margins can change the selected update. We introduce PIVOT-KG, a paired differential validator that estimates the correction between proxy and deployment improvement and allocates a fixed high-fidelity evaluation budget by expected reduction in update-selection regret per unit cost. Across controlled response worlds and 90 held-out decision roots, global policy ranking can remain stable as update-level fidelity deteriorates, and proxy and deployment top-1 updates disagree in 51 of 90 roots. Yet response magnitude and ranking flips alone do not predict validation value: PIVOT-KG helps when high-fidelity evidence can resolve uncertainty that matters for the update decision. These results identify Improvement Fidelity and decision-relevant validation as central to reliable self-improvement in adaptive worlds.
\end{abstract}


\section{Introduction}

Self-improvement is usually written as a loop: generate candidates, score
them, promote one, and repeat.
A self-improvement update is a replacement operation, not an isolated policy
value. In a trading system, a
larger order changes liquidity; in an auction, a bid changes rivals'
responses; in a recommender, a policy changes future user behavior. A verifier
can therefore rank policies correctly while ranking improvements incorrectly.
High-fidelity deployment tests can expose the error, but they are often too
costly to run on every candidate.

We study a proposed transition $\tau=(\pi,\pi')$. Let $V$ denote the cheap
verifier world and let $*$ denote the policy-induced deployment world. Define
\[
  \Delta_V=J_V(\pi')-J_V(\pi),\qquad
  \Delta_*=\mathcal J(\pi')-\mathcal J(\pi).
\]
An improvement reversal, $\Delta_V>0$ but $\Delta_*<0$, is one salient
failure mode. More generally, deployment can change the sign or ordering of
candidate improvements, and a fidelity error matters to selection when it
changes the selected update or displaces the no-update action.
Figure~\ref{fig:reversal} shows the basic sign failure.

\begin{figure}[t]
  \centering
  \includegraphics[width=\linewidth]{fig1_improvement_reversal.pdf}
  \caption{Paired proxy and deployment improvements in the two controlled
  worlds. The lower-right quadrant is an improvement reversal. Points are a
  deterministic display subsample; IRR is computed on all transition rows.}
  \label{fig:reversal}
\end{figure}

We distinguish three response layers. The \emph{direct} layer holds the
incumbent world fixed; the \emph{actor} layer lets the environment respond
mechanically to the candidate; the \emph{strategic} layer additionally lets
other agents adapt. These correspond to progressively stronger deployment
responses.

We call the agreement between proxy and deployment improvement over the
updates an agent actually proposes \emph{Improvement Fidelity}. It is
operator-relative: the relevant population is the set of updates generated by
the self-improvement process. Because only some fidelity errors change the
update decision, limited high-fidelity evaluation should be targeted to
uncertainty that matters for selection.

Our contributions are:
\begin{description}
  \item[Contribution 1.] We formalize operator-relative Improvement Fidelity
  for self-improving agents and show why accurate global policy evaluation can
  still fail on the updates an agent actually proposes. Our analysis links
  these failures to operator shift, deployment response, and candidate
  margins.

  \item[Contribution 2.] We introduce PIVOT-KG, a paired differential
  validator that allocates a limited high-fidelity evaluation budget according
  to the expected reduction in update-selection regret per unit cost.

  \item[Contribution 3.] We evaluate Improvement Fidelity across controlled
  direct, actor, and strategic response worlds and held-out decision roots,
  showing when response creates update-level fidelity errors, when those
  errors change the selected update, and when targeted high-fidelity
  validation reduces selection regret.
\end{description}

Together, these contributions provide a direct way to diagnose when self-improvement survives deployment and to spend scarce validation effort where it can affect the update decision.

\section{Related Work}
\paragraph{Performative and strategic response.}
Performative prediction and reinforcement learning make deployment part of
the data-generating process
\cite{perdomo2020performative,mandal2023performative,rank2024performative,
mandal2025linear,gois2025performative,multiagentperformative2022}.
These works study learning, stability, and equilibrium behavior under
deployment-induced response; we ask whether a particular proposed update
preserves its sign and ordering after that response is realized.

\paragraph{World models and policy evaluation.}
World models and policy-aware simulators evaluate values, rankings, or
trajectory fidelity
\cite{worldgym2025,policyaware2026,tdmpc22024}.
Global value or ranking fidelity can coexist with a wrong local update sign,
which is the gap measured here.

\paragraph{Self-improvement and verification.}
Autonomous policy evolution and verification make iterative self-improvement
operational \cite{evopolicygym2026,dgm2025,selfauthored2026,wu2024progress}.
We focus on a complementary failure mode: a fixed external verifier can
evaluate the wrong response world, so a proposed replacement need not remain
an improvement after deployment.

\paragraph{Adaptive selection and paired evaluation.}
Active learning, knowledge gradients, and best-arm identification allocate
expensive observations \cite{settles2009active,lindley1956information,
frazier2009kg,chick2001selection,kalyanakrishnan2012pac}. PIVOT changes the
loss from global prediction error to update-selection regret and uses paired
rollouts to cancel common-mode variation.

\section{Problem Formulation}

\paragraph{Policy-induced worlds and response levels.}
Let $\M[\pi]$ denote the deployment world induced by policy $\pi$, including
any environment response to that policy, and write
$\J(\pi)=J(\pi;\M[\pi])$. We consider three response levels:
\begin{align*}
 \Delta_{\mathrm{direct}} &= J(\pip;\M[\pii])-J(\pii;\M[\pii]),\\
 \Delta_{\mathrm{actor}} &= J(\pip;\M[\pip])-J(\pii;\M[\pii]),\\
 \Delta_{\mathrm{strategic}} &=
 J_i(\pip,R_{-i}(\pip))-J_i(\pii,R_{-i}(\pii)).
\end{align*}
The direct level holds the incumbent world fixed, the actor level allows the
environment to respond to the candidate, and the strategic level additionally
allows other agents to adapt. Here $R_{-i}$ denotes a general response map for
the other agents; best response is one special case.

\paragraph{Improvement Fidelity.}
Let an improvement operator $\mathcal A$ induce a transition law
$Q_{\mathcal A}$ over proposed updates $\tau=(\pi,\pi')$. For loss $L$, we
define operator-relative Improvement Fidelity as
\begin{equation}
  \operatorname{IF}(V,\mathcal A;L)=
  \E_{(\pi,\pi')\sim Q_{\mathcal A}}
  [L(\Delta_V,\Delta_*)].
  \label{eq:if}
\end{equation}
The relevant population is therefore the updates the agent actually proposes,
rather than the policy space in general. Improvement Fidelity is an
update-level quantity: accurate policy values or rankings do not by themselves
guarantee accurate improvement estimates.

\paragraph{Core metrics.}
For transition rows, we measure fidelity-error magnitude by
$\mathrm{IDE}=n^{-1}\sum_k|\Delta_{V,k}-\Delta_{*,k}|$ and let
$\mathrm{ISC}$ denote sign agreement after removing ties. A salient sign
failure is the improvement reversal rate
$\mathrm{IRR}=P(\Delta_*<0\mid\Delta_V>0)$. For a candidate set $C$, the
decision consequence is measured by improvement-selection regret,
\begin{equation}
  \mathrm{ISR}=
  \max_{j\in C}\Delta_{*,j}-\Delta_{*,\hat j},
  \label{eq:isr}
\end{equation}
where $\hat j$ is the selected candidate. Thus IDE measures error magnitude,
ISC and IRR measure sign preservation, and ISR measures the consequence for
update selection.
\section{Properties of Improvement Fidelity}

The following results characterize when policy-level evaluation controls
update-level fidelity, how operator shift and deployment response can break
that connection, and what a high-fidelity validator must resolve.

\begin{proposition}[Uniform value fidelity is sufficient]
If $\sup_\pi|J_V(\pi)-\J(\pi)|\leq\varepsilon$, then
$|\dproxy-\dtrue|\leq2\varepsilon$. The sign is preserved whenever
$|\dtrue|>2\varepsilon$.
\end{proposition}
\begin{proof}
$\dproxy-\dtrue=[J_V(\pip)-\J(\pip)]-[J_V(\pii)-\J(\pii)]$;
the triangle inequality gives the bound and the sign statement.
\end{proof}

\begin{proposition}[Global policy accuracy need not imply update fidelity]
For every $\varepsilon>0$, there are a finite policy family, a verifier, a
policy distribution, and an update operator with policy MAE and rank error
below $\varepsilon$, but $\mathrm{IRR}=1$ and $\mathrm{ISC}=0$ on the
operator's transition law.
\end{proposition}
\begin{proof}
Take policies $\pi_0,\ldots,\pi_{n-1}$ with $\J(\pi_k)=k/(n-1)$ and swap one adjacent pair in $V$.
Then $\mathrm{MAE}=2/[n(n-1)]$ and
$1-\mathrm{Spearman}=12/[n(n^2-1)]$, both tending to zero. Let the operator
sample only the swapped directed transition and propose the lower-valued true
policy. Its proxy sign is positive and its deployment sign is negative.
\end{proof}

\begin{proposition}[Operator Shift Bound]\label{prop:operator-shift}
Let $P$ be a calibration law, let $Q_{\mathcal A}\ll P$ have density ratio
$w=q_{\mathcal A}/p$, and let $\ell(\tau)=L(\Delta_V,\Delta_*)$ have finite
variance under $P$. Then
\begin{equation}
 \E_{Q_{\mathcal A}}[\ell]
 \leq
 \E_P[\ell]
 +\sqrt{\operatorname{Var}_P(\ell)\,
 \chi^2(Q_{\mathcal A}\Vert P)}.
 \label{eq:operator-shift}
\end{equation}
\end{proposition}
\begin{proof}
Write
$\E_{Q_{\mathcal A}}[\ell]-\E_P[\ell]
=\E_P[(w-1)(\ell-\E_P[\ell])]$.
Cauchy--Schwarz and
$\E_P[(w-1)^2]=\chi^2(Q_{\mathcal A}\Vert P)$
give the result.
\end{proof}

Deployment response provides a second source of update-level error. From the
definitions in the previous section,
\[
 \Delta_{\mathrm{actor}}-\Delta_{\mathrm{direct}}
 =
 J(\pip;\M[\pip])-J(\pip;\M[\pii]).
\]
Thus the actor--direct gap is exactly the change in the candidate's value
caused by the world it induces. The strategic level extends this response to
adaptive opponents through $R_{-i}$.

\begin{lemma}[Decision preservation]\label{lem:decision-preservation}
For candidates $j,k$ with true margin
$m=\Delta_{*,j}-\Delta_{*,k}>0$, if each estimated improvement has absolute
error below $m/2$, then the estimated ordering of $j$ and $k$ is unchanged.
\end{lemma}
\begin{proof}
The perturbation of the estimated margin has absolute value below $m$, so the
estimated margin remains positive.
\end{proof}

\begin{lemma}[Paired variance]
For a paired rollout difference $Y'-Y$,
\[
  \operatorname{Var}(Y'-Y)
  =
  \operatorname{Var}(Y')
  +\operatorname{Var}(Y)
  -2\operatorname{Cov}(Y',Y).
\]
When shared randomness induces positive covariance, paired evaluation has
lower variance than independent evaluation with the same marginal variances.
\end{lemma}

Together, these results narrow the validation problem. Uniformly accurate
policy values control update error, but small global MAE or rank error need
not protect the transitions generated by the improvement operator. Operator
shift and deployment response can therefore create local fidelity errors,
while only errors large relative to candidate margins can change the selected
update. Paired high-fidelity rollouts reduce the variance of estimating these
differences when common randomness induces positive covariance. This motivates
PIVOT-KG: allocate a limited high-fidelity budget to update uncertainty that
can affect selection.
\section{PIVOT-KG}

The preceding results suggest a simple validation principle: spend expensive
high-fidelity evaluations on update uncertainty that can change the selection
decision. We implement this principle with PIVOT-KG. Given an incumbent and
$K$ candidate updates, the cheap verifier first provides proxy improvements
$\{\Delta_{V,j}\}_{j=1}^K$.

Rather than model deployment improvement from scratch, PIVOT-KG models the
differential correction
\[
  G_j=\Delta_{*,j}-\Delta_{V,j}.
\]
Writing $G=(G_1,\ldots,G_K)$, $\Delta_V=(\Delta_{V,1},\ldots,\Delta_{V,K})$,
and $\Delta_*=(\Delta_{*,1},\ldots,\Delta_{*,K})$, we place a joint Gaussian
prior over the correction vector,
\[
  G\sim\mathcal N(\mu,\Sigma_G),\qquad
  \Delta_*\mid\Delta_V\sim
  \mathcal N(\Delta_V+\mu,\Sigma_G).
\]
The prior mean, covariance, and observation variances are estimated from a
disjoint calibration set of paired proxy/high-fidelity transitions before
held-out evaluation. Thus a new held-out decision root starts from the
calibrated posterior $\Delta_V+\mu$ without using its high-fidelity outcomes.

For a queried candidate $j$, let $Y_j$ denote its paired high-fidelity
improvement observation,
\[
  Y_j=\Delta_{*,j}+\epsilon_j,
  \qquad
  \epsilon_j\sim\mathcal N(0,s_j^2)
\]
where $s_j^2$ is estimated from calibration data. The incumbent and candidate
are evaluated under the same initial state, exogenous randomness, and opponent
initialization. Each query updates the joint posterior by Gaussian
conditioning; with non-diagonal $\Sigma_G$, evidence can also update correlated
unqueried candidates.

We augment the candidate set with a no-update action,
$C^+=\{0,1,\ldots,K\}$, where $\Delta_{*,0}=0$ denotes retaining the
incumbent. Let $D$ denote the paired high-fidelity evidence collected at the
current decision root. The current decision is
\[
  a(D)=\arg\max_{j\in C^+}
  \E[\Delta_{*,j}\mid D].
\]
Its posterior expected simple regret is
\[
  \mathcal R(D)=
  \E\left[
    \max_{j\in C^+}\Delta_{*,j}
    -\Delta_{*,a(D)}
    \,\middle|\,D
  \right].
\]
For a hypothetical high-fidelity observation $Y_j$, define
\begin{equation}
  \mathrm{EVSI}_j
  =
  \mathcal R(D)
  -
  \E_{Y_j\mid D}
  \bigl[\mathcal R(D\cup\{Y_j\})\bigr],
  \qquad
  A_j=\frac{\mathrm{EVSI}_j}{c_j}.
  \label{eq:acquisition}
\end{equation}
Thus $\mathrm{EVSI}_j$ measures the expected reduction in update-selection
regret from querying candidate $j$, while dividing by $c_j$ prioritizes the
largest expected regret reduction per unit HF cost. This one-step expected
value of sample information gives the knowledge-gradient acquisition used by
PIVOT-KG.

\begin{algorithm}[H]
\caption{PIVOT-KG under a fixed high-fidelity budget}
\label{alg:pivot-kg}
\begin{algorithmic}[1]
\REQUIRE Incumbent $\pi$; candidates $\{\pi'_j\}_{j=1}^K$;
calibrated correction prior $p(G)$; query costs $\{c_j\}_{j=1}^K$;
HF budget $B$
\ENSURE Selected candidate or incumbent
\STATE Evaluate candidates with the cheap verifier to obtain
$\{\Delta_{V,j}\}_{j=1}^K$
\STATE Initialize $D\leftarrow\emptyset$, queried set
$Q\leftarrow\emptyset$, remaining budget $b\leftarrow B$, and the calibrated
posterior over $\Delta_*$
\STATE Include the no-update action $j=0$ with $\Delta_{*,0}=0$
\WHILE{there exists $j\notin Q$ with $c_j\leq b$}
  \FOR{each feasible unqueried candidate $j$}
    \STATE Compute $\mathrm{EVSI}_j$ and $A_j\leftarrow\mathrm{EVSI}_j/c_j$
  \ENDFOR
  \STATE $j_q\leftarrow\arg\max_j A_j$
  \STATE Obtain paired HF observation $Y_{j_q}$ under matched randomness
  \STATE $D\leftarrow D\cup\{Y_{j_q}\}$ and update the posterior by Gaussian conditioning
  \STATE $Q\leftarrow Q\cup\{j_q\}$ and $b\leftarrow b-c_{j_q}$
\ENDWHILE
\STATE \RETURN
$\displaystyle a(D)=\arg\max_{j\in C^+}\E[\Delta_{*,j}\mid D]$
\end{algorithmic}
\end{algorithm}

The primary comparison uses this fixed-budget rule; posterior-based early
stopping is a secondary variant. Unlike global label acquisition, PIVOT-KG
values a high-fidelity query by its expected reduction in update-selection
regret.
\section{Experiments}
The evaluation follows the progression from update-level fidelity error, to
its consequences for update selection, to the value of targeted high-fidelity
validation. We first test when global evaluation fails on proposed updates,
then whether those errors alter the selected update, and finally whether
PIVOT-KG can use limited high-fidelity evidence to reduce selection regret.

\subsection{Experimental Setup}
Controlled response worlds use paired common-random-number rollouts and 30
independent seeds. The seed, trajectory, opponent-seed cluster, or held-out
root is the inferential unit; transition rows are retained for audit and are
not treated as independent replicates. The sealed study contains 30 held-out
roots in each of Leduc, Kuhn, and Melting Pot, with 1,800 sealed decisions per
cohort. We use Leduc and Kuhn poker through OpenSpiel~\cite{lanctot2019openspiel}
and the Melting Pot multi-agent suite~\cite{leibo2021meltingpot}. Primary
sealed comparisons allocate a fixed HF query budget and compare PIVOT-KG with
Uniform; matched-cost analyses additionally include Proxy Only, Global-VOI,
Paired LUCB, and an all-HF reference where applicable. HighwayEnv is evaluated
separately as a physically reactive external setting. Reported bootstrap
intervals resample the corresponding inferential unit. Additional statistical
and reproducibility details are included in the supplement.

\subsection{Improvement Fidelity under Operator Shift and Deployment Response}
Two mechanisms can break update-level fidelity. Operator shift changes the
transition distribution induced by the improvement process, while deployment
response changes the world in which the candidate is evaluated. In the
operator-shift study, the high-minus-low shift contrast in operator-relative
IDE is \OperatorShiftEffect{} with bootstrap interval
\OperatorShiftEffectCI{} across \OperatorShiftSeeds{} seeds and
\OperatorShiftRows{} paired rows (Figure~\ref{fig:operator-shift}). Local
update error and reversal rates increase while global policy rank remains
comparatively stable, showing that strong global evaluation can mask failure
on the updates an operator actually proposes.

\begin{figure}[H]
  \centering
  \includegraphics[width=\linewidth]{fig2_operator_shift.pdf}
  \caption{Operator-relative shift diagnostic. Local improvement error rises
  with registered shift while global policy rank remains comparatively stable.
  The same environment-by-shift cells show error, rank, and reversal rate.}
  \label{fig:operator-shift}
\end{figure}

Deployment response provides a distinct source of error. The direct, actor,
and strategic levels progressively expose mechanical environment response and
adaptive opponents. In the strategic fixture, the strategic-minus-actor
effect is \StrategicEffect{} \StrategicEffectCI{}, with a family-balanced
strategic reversal rate of \StrategicSIRR{}. Extended world-response
decompositions and opponent-family results appear in
Appendix~\ref{app:fidelity-response}. Together, these experiments identify two
distinct sources of update-level fidelity error: operator shift and
policy-induced deployment response.

\subsection{From Fidelity Error to Update Decisions}
Lemma~\ref{lem:decision-preservation} connects update error to selection: an error
changes an ordering only when it is large relative to the candidate margin.
For each sealed root we therefore compute the deployment top-1 margin, the
proxy-to-deployment correction, and the largest pairwise
correction-to-margin ratio. A ratio at least one marks a proxy/deployment
top-1 flip.

Across the \RevisionDecisionRoots{} sealed roots, \RevisionDecisionFlips{}
have a top-1 flip (\RevisionLeducFlips{} Leduc, \RevisionKuhnFlips{} Kuhn,
and \RevisionMeltingFlips{} Melting Pot). To compare across reward scales,
Figure~\ref{fig:decision-relevance} reports each root's squared response gap
normalized by the square of its largest candidate range across proxy and
long/short deployment deltas. Top-1 flips alone do not predict validation
value: normalized allocation gain is \RevisionFlippedNormalizedGain{}
\RevisionFlippedNormalizedGainCI{} on flipped roots and
\RevisionStableNormalizedGain{} \RevisionStableNormalizedGainCI{} on stable
roots. Thus response magnitude and decision relevance are not the same as
allocation value.

The next question is whether available HF evidence can resolve the fragile
decision. For each root, the ex-post decision-critical pair is the union of
the proxy and deployment top-1 candidates, while the proxy top-2 margin
measures the separation of the leading proxy candidates. At the primary long
budget, the PIVOT query set intersects the decision-critical pair in
\RevisionCriticalQueryRate{} \RevisionCriticalQueryRateCI{} of roots, but the
mean queried HF noise is \RevisionCriticalNoiseMargin{}
\RevisionCriticalNoiseMarginCI{} times the proxy top-2 margin. Thus targeting
a decision-critical candidate does not by itself guarantee that the available
HF evidence can resolve the ordering. Additional root-level diagnostics appear
in Appendix~\ref{app:pivot-ext}.

\begin{figure}[H]
  \centering
  \includegraphics[width=\linewidth]{fig1_decision_relevance.pdf}
  \caption{Response magnitude is not allocation value. Panel a reports
  within-root normalized response gaps; b reports proxy/deployment top-1
  flips; c reports normalized ISR reduction; d relates correction-to-margin
  ratio to normalized ISR reduction; e relates queried HF noise to the proxy
  top-2 margin. ISR reductions are divided by the within-root deployment
  range. In e, filled circles denote queries intersecting the ex-post
  decision-critical pair and open diamonds denote misses.}
  \label{fig:decision-relevance}
\end{figure}

Together, the sealed analysis shows that fidelity error creates validation
value only when it threatens the update decision and the available HF evidence
can resolve that uncertainty.

\subsection{PIVOT-KG under Limited High-Fidelity Budgets}
Table~\ref{tab:latest-main} reports the sealed fixed-budget PIVOT-KG versus
Uniform comparison. Leduc has a positive primary contrast, Kuhn is near-null,
and the Melting Pot primary condition is unresolved with mixed secondary
conditions. The pattern is consistent with the preceding mechanism analysis:
a larger response gap does not by itself create allocation value.

\begin{table}[H]
  \centering\footnotesize
  \caption{Sealed response-world allocation contrasts ($n=30$ paired roots per row). Uniform ISR and
  PIVOT-KG ISR are mean improvement-selection regrets; Uniform $-$ PIVOT is
  the ISR reduction from targeted validation, so positive values favor
  PIVOT-KG. Intervals are
  root-bootstrap 95\% intervals. Group headings identify preregistered
  primary and secondary comparisons.}\label{tab:latest-main}
  \input{tables/revision_main_results.tex}
\end{table}

For iterative self-improvement, we report cumulative improvement-selection
regret, $\mathrm{CISR}=\sum_{t=1}^{T}\mathrm{ISR}_t$. Across
\ClosedLoopRounds{} rounds, PIVOT-KG reduces paired CISR relative to Proxy
Only by \ClosedLoopEffect{} \ClosedLoopEffectCI{}. Matched-cost comparisons
with Global-VOI and Paired LUCB vary across settings; Figure~\ref{fig:efficiency}
shows the resulting evidence-efficiency frontier together with an all-HF
reference. Detailed closed-loop trajectories and robustness analyses appear in
Appendix~\ref{app:pivot-ext}.

\begin{figure}[H]
  \centering
  \includegraphics[width=\linewidth]{fig4_evidence_efficiency.pdf}
  \caption{Evidence-efficiency frontier. Matched-cost curves and the
  fixed-budget forest report selection-regret reduction; the all-HF line is a
  reference. Intervals are bootstrap intervals over trajectory seeds.}
  \label{fig:efficiency}
\end{figure}

Overall, PIVOT-KG is useful when the available HF evidence can resolve
uncertainty that matters for the update decision, rather than whenever
deployment response is large.

\subsection{Stress Tests Beyond Controlled Environments: HighwayEnv}
To test PIVOT-KG in a physically reactive setting where high-fidelity
evaluation is genuinely scarce relative to the candidate set, we use a
separately registered HighwayEnv redesign with eight distinct scheduled
candidates, formed by crossing four non-idle actions with early and late
half-horizon windows. The redesign fits the correction model on 40 calibration
roots and evaluates 120 disjoint test roots. At the preregistered primary
budget $B=4$, PIVOT-KG reduces ISR relative to exact Uniform allocation by
\HighwayRedesignBudgetFourContrast{} \HighwayRedesignBudgetFourCI{}; the
secondary $B=2$ contrast is
\HighwayRedesignBudgetTwoContrast{} \HighwayRedesignBudgetTwoCI{}. Uniform
is averaged exactly over all feasible queried subsets, and each PIVOT query
corresponds to a physical paired actor rollout. Earlier five-action cohorts
were unresolved at their registered primary budgets and are reported in
Appendix~\ref{app:highway-replication}. We therefore treat the redesign as
external evidence that targeted validation can help when candidate-set
structure creates a meaningful allocation problem, not as a universal
HighwayEnv advantage. Additional OOD-evaluator and public-data boundary
analyses appear in Appendix~\ref{app:boundary}.

\section{Conclusion}
An agent can be correct about the wrong world. This paper argues that reliable
self-improvement requires evaluating proposed policy replacements at the
update level, under the response they induce. Improvement Fidelity formalizes
whether proxy improvement preserves deployment improvement on the updates an
agent actually proposes, and the analysis shows how operator shift,
deployment response, and candidate margins determine when policy-level
accuracy is insufficient. PIVOT-KG targets the resulting decision problem by
allocating limited paired HF evidence according to its expected reduction in
update-selection regret. Across controlled, sealed, and external settings,
response magnitude alone does not determine either ranking failure or the
value of additional validation.

Several boundaries remain. The direct, actor, and strategic response worlds
cover selected forms of adaptation rather than all deployment dynamics, and
the finite strategic fixtures are not equilibrium characterizations. PIVOT-KG
also depends on the informativeness of HF evidence: limited budgets or noisy
measurements can leave decision-relevant uncertainty unresolved. Finally,
earlier five-action HighwayEnv cohorts are unresolved at their registered
primary budgets, while the public-finance analysis is observational and does
not identify a causal deployment response. The eight-candidate HighwayEnv
redesign has a positive primary $B=4$ contrast in a panel deliberately large
enough to create a nontrivial allocation problem, but its claim is limited to
that registered panel and budget allocation.

These boundaries point to richer response models, repeated settings in which
the response itself evolves with the agent, and intervention-based HF evidence
in more realistic deployment systems. More broadly, self-improvement should
be judged not only by whether a candidate scores better under its verifier,
but by whether the proposed replacement remains better in the world it
creates. Reliable self-improvement therefore requires fidelity of
improvements, not merely accuracy of policy evaluations.\label{main:end}

\section*{AI Use Statement}
The original version of this manuscript was written entirely by the authors.
The research questions, conceptual framing, mathematical development, and
experimental design were developed by the authors. Generative AI tools were
used only during manuscript revision for language and presentation polishing;
they were not used to formulate the research ideas, design the experiments, or
generate numerical results. All reported experimental results were produced by
the authors' executed research code and were checked and verified by the
authors. The authors reviewed the final manuscript and take full responsibility
for its contents.

\paragraph{Reproducibility Statement.}
The anonymous supplement contains deterministic configurations, hash-indexed
source evidence, figure tables, and rebuild commands. The public-finance audit
uses virtual fills and contains no credentials, private data, raw vendor
archives, or live orders.

\paragraph{Ethics Statement.}
The study uses controlled virtual environments and public market data only; it
involves no human subjects, personal data, live trading, or external
authorization.

\label{refs:start}
\bibliographystyle{iclr2027_conference}
\bibliography{references}

\clearpage
\appendix
\section*{Appendix}
The appendix follows the main evaluation. Appendix~\ref{app:fidelity-response}
extends the response analyses from Section~6.2; Appendix~\ref{app:pivot-ext}
contains detailed PIVOT-KG and robustness results supporting Sections~6.3--6.4;
Appendix~\ref{app:highway-replication} collects the complete HighwayEnv
historical-cohort and redesign audit;
Appendix~\ref{app:boundary} reports boundary analyses. The supplement
contains the statistical and reproducibility details, including raw sampled reversal-rate cells and operator-shift cells.

\section{Extended Fidelity and Response Analyses}\label{app:fidelity-response}
The main text separates operator shift from policy-induced deployment response.
Here we expand the response side of that analysis. The world-response
decomposition traces matched observations through the direct, actor, and
strategic levels, while the opponent-family analysis shows how the strategic
effect varies across adaptive mechanisms. For the latter, the reported
family-balanced strategic reversal rate is the unweighted mean across the
three adaptive opponent families.

\begin{figure}[H]
  \centering
  \includegraphics[width=\linewidth]{figA_response_footprint.pdf}
  \caption{World-response decomposition. Panel a traces
  \StrategicAllFamilySeedTraces{} family-by-seed observations (five opponent
  families $\times$ 30 matched seeds) through direct, actor, and strategic
  worlds; summary intervals use the 30 matched seeds. Panel b separates
  mechanical and strategic effects; panel c marks the strategic reversal
  quadrant.}
  \label{fig:layers}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=\linewidth]{figD_strategic_distribution.pdf}
  \caption{Strategic generalization across opponent mechanisms. Raincloud and
  forest views show family-specific effects over 30 matched
  opponent-seed clusters. The reversal plane labels the unweighted mean across
  the three adaptive families and uses both color and marker shape for
  grayscale legibility.}
  \label{fig:strategic}
\end{figure}

\section{Extended PIVOT-KG Evaluation}\label{app:pivot-ext}
The main text reports the sealed allocation results and the matched-cost
frontier. Here we provide additional decision-resolvability diagnostics,
trajectory-level results, and numerical-robustness views. The within-root
normalized response gaps are \RevisionLeducNormalizedResponseGap{}
\RevisionLeducNormalizedResponseGapCI{} for Leduc,
\RevisionKuhnNormalizedResponseGap{} \RevisionKuhnNormalizedResponseGapCI{}
for Kuhn, and \RevisionVFourNormalizedResponseGap{}
\RevisionVFourNormalizedResponseGapCI{} for Melting Pot. At the primary long
budget, the decision-critical query-hit rates are
\RevisionLeducCriticalQueryRate{} in Leduc, \RevisionKuhnCriticalQueryRate{}
in Kuhn, and \RevisionMeltingCriticalQueryRate{} in Melting Pot. The
descriptive normalized gain is \RevisionCriticalQueryHitGain{}
\RevisionCriticalQueryHitGainCI{} among the 87 hit roots and
\RevisionCriticalQueryMissGain{} \RevisionCriticalQueryMissGainCI{} among the
three misses; the final sealed estimate separation is
\RevisionPostQuerySeparation{} \RevisionPostQuerySeparationCI{} after root
scaling. For the closed-loop plots, CTI denotes cumulative true improvement and
CISR denotes cumulative improvement-selection regret.

\begin{figure}[H]
  \centering
  \includegraphics[width=\linewidth]{fig5_closed_loop.pdf}
  \caption{Closed-loop validator outcomes. The top row reports cumulative true
  improvement (CTI) and the bottom row cumulative improvement-selection regret
  (CISR); thin curves show seed-level trajectories and markers summarize the
  compared decision rules.}
  \label{fig:closed-loop}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=\linewidth]{figC_posterior_robustness.pdf}
  \caption{PIVOT-KG sensitivity to posterior sample count and cost
  misspecification across the registered calibration groups.}
  \label{fig:robustness}
\end{figure}

\section{HighwayEnv Validation, Replication, and Redesign}\label{app:highway-replication}
The original and replication cohorts each contain 20 calibration and 60 test
roots, with no overlap between cohorts. The replication freezes the five-action
panel, four lanes, 20 traffic vehicles, horizon 20, and covariance shrinkage
0.5. It refits the correction prior on its own calibration cohort before
opening the full test cohort. The primary contrast is Uniform minus PIVOT-KG
ISR at $B=2$; $B=4$ is secondary. We retain every registered root and use
4,000 paired bootstrap resamples with fixed RNG seeds. Individual 95\%
intervals condition on each fitted calibration model and do not include
calibration sampling uncertainty.

A technical smoke run exposed the first two replication test roots before the
full run; those roots are retained without subsequent tuning or exclusions.
The supplement contains the execution note, complete seed records, query and
truth ledgers, dependency lock, frozen simulator code, and a standalone
recomputation command. Calibration and post-decision truth audits are outside
the logical query budget and are included in physical evaluation accounting.
Uniform HF retains proxy estimates for unqueried candidates, whereas PIVOT-KG
uses its calibrated posterior; the comparison therefore evaluates the complete
selection rules rather than acquisition alone.

\paragraph{Eight-candidate redesign.}\label{app:highway-redesign}
The redesign freezes eight distinct scheduled candidates, crossing the four
non-idle HighwayEnv actions with early and late half-horizon windows. It fits a
joint correction model on 40 calibration roots and evaluates 120 disjoint test
roots. The primary budget is $B=4$; $B=2$ is secondary. Uniform is the exact
average over all $\binom{8}{B}$ queried subsets (70 at $B=4$, 28 at $B=2$),
whereas PIVOT-KG uses sequential exact affine-normal Gaussian KG updates.
Each selected query is a physical paired actor rollout. All candidate truth
outcomes, including the shared incumbent, are evaluated only after the
adaptive decisions and are excluded from the logical query budget. The audit
checks all 3,840 physical evaluations and uses 4,000 paired bootstrap
resamples with fixed seeds. Two test roots were exposed during technical
smoke; they remain in the formal cohort without retuning or exclusion, so this
is external DEV evidence rather than a wholly blinded confirmatory test.
The supplement contains the hash-indexed evidence, frozen redesign source,
query/truth ledgers, and independent table/figure audit.

\begin{table}[H]
  \centering\footnotesize
  \caption{Original HighwayEnv study, fresh-seed replication, and the
  eight-candidate redesign. $B$ counts candidate paired HF queries per
  decision; roots are independent paired test seeds. Uniform $-$ PIVOT is
  the ISR reduction from targeted validation, so positive values favor
  PIVOT-KG; intervals are paired 95\%
  bootstrap intervals. Bold budgets mark the registered primary comparison:
  $B=2$ in the original/replication cohorts and $B=4$ in the redesign.}
  \label{tab:highway-budget}
  \input{tables/highway_results.tex}
\end{table}

\begin{figure}[H]
  \centering
  \includegraphics[width=\linewidth]{fig4_highway_budget.pdf}
  \caption{HighwayEnv paired allocation contrasts across the historical,
  fresh-seed, and eight-candidate redesign cohorts. Positive
  Uniform-minus-PIVOT ISR favors PIVOT-KG; error bars are paired 95\%
  bootstrap intervals over the registered test seeds.}
  \label{fig:highway-budget}
\end{figure}

\section{Boundary Analyses}\label{app:boundary}
\paragraph{OOD evaluator comparison.}
The powered OOD transition-evaluator comparison gives transition-minus-global
ISC of \OODGain{} \OODGainCI{}. This result separates the update-level
estimand from any claim that a transition-specific learner must dominate a
global learner.

\begin{figure}[H]
  \centering
  \includegraphics[width=\linewidth]{figB_learned_ood_null.pdf}
  \caption{OOD evaluator comparison. Forest horizontal ranges are descriptive
  spans across the two fitted model families in each split; the scatter
  retains paired reports and the diagonal indicates equal ISC.}
  \label{fig:ood-null}
\end{figure}

\paragraph{Public-finance diagnostic.}
The public-data audit uses historical paths, virtual fills, and a
participation/depth proxy. It has 0/7 depth-proxy reversals in the expanded
primary sessions and 0/5 in the holdout; the pooled depth-minus-replay effect
is approximately $-4.16\times10^{-7}$ in native session units. Historical
paths do not identify the counterfactual market response to deploying a
different policy, so this analysis serves as an observational boundary check.

\begin{figure}[H]
  \centering
  \includegraphics[width=\linewidth]{figE_finance_boundary.pdf}
  \caption{Initial single-asset public-finance boundary diagnostic over
  participation. Shading is a seven-session descriptive interval; fills are
  virtual. The separate 12-session, three-asset expansion is summarized in
  Table~\ref{tab:public-boundary}.}
  \label{fig:finance}
\end{figure}

\begin{table}[H]
\centering\scriptsize
\caption{Public-data boundary summary.}\label{tab:public-boundary}
\input{tables/public_results.tex}
\end{table}

\begin{table}[H]
\centering\scriptsize
\caption{Controlled evidence summary.}\label{tab:controlled-summary}
\input{tables/controlled_results.tex}
\end{table}

\end{document}
