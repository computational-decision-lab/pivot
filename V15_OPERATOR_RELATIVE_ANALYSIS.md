# Operator-Relative Analysis

DEV operator counts are retained separately: `2`
proposal families and `4` candidate records.
The confirmatory operator-relative comparison is **NOT_RUN**; no pooled claim
is made from the DEV rows.
