# Modern Agent Evaluation Implementation Plan

1. Freeze the current submission and record commit, tree, artifact, figure,
   and configuration hashes.
2. Add immutable protocol, candidate, transition, and sealed-plane schemas;
   write tests for hash stability, role isolation, and paired sandbox starts.
3. Add two proxy-only proposal operators and a local DEV reference runner;
   write canonical transition/candidate tables and provenance manifests.
4. Add optional control-plane adapters and a pre-outcome confirmatory lock for
   Inspect AI, mini-SWE, and Pi.  Report unavailable external dependencies as
   an explicit boundary.
5. Add the V15 command surface, claim registry, resource plan, terminal-state
   reports, and reviewer-attack audit.  Run DEV smoke only; never promote it
   to the paper's confirmatory evidence.
6. Remove internal result-macro/path labels from the manuscript source, update
   the strategic-family sample-size wording, and rebuild the PDF and
   supplementary archive.
7. Build neutral figure bundles, perform machine and rendered visual checks,
   run number/claim/reference/anonymity/language/reproducibility audits, and
   stop with an honest final status if an external confirmatory gate remains
   open.
