# 同事交接清单

## A. 先理解项目

- [ ] 阅读 `docs/zh/00_项目入口与当前状态.md`、`01_研究问题与理论.md`、`02_算法与代码架构.md`。
- [ ] 阅读 `docs/archive/v15/V15_FINAL_REPORT.md`、`docs/archive/v15/V15_IMPLEMENTATION_MATRIX.md` 和 `release/v15/submission_verification.json`。
- [ ] 确认 V9 冻结证据、V15 DEV 证据和 V15 确认性 `NOT_RUN` 分属不同证据层。

## B. 本地复核

- [ ] 运行 `.venv/bin/pytest -q` 和 `.venv/bin/ruff check src scripts experiments tests`。
- [ ] 检查 `src/pivot/core/transition.py`、`src/pivot/evaluation/paired.py`、`src/pivot/acquisition/pivot_voi.py` 和 `src/pivot/algorithms/pivot.py`。
- [ ] 用 ImprovementBench v1 完成一次输出到 `/tmp` 的 smoke，并核对 manifest。

## C. 外部确认性运行前

- [ ] 核对 `configs/v15/confirmatory.yaml`、runtime manifest、镜像 digest、依赖锁和 sealed task manifest hash。
- [ ] 确认模型、沙箱、预算、任务平面和 seed 已在结果前冻结。
- [ ] 确认授权和外部资源可用；DEV override 不得进入确认性命令。

## D. 执行顺序

1. mini-SWE transition audit。
2. promotion replay 与 closed-loop assessment。
3. Pi cross-scaffold replication。
4. identity-blind strategic response。
5. 注册 ablations。

每阶段写独立结果目录，生成 canonical artifacts、terminal-state audit 和 claim audit；阶段失败也必须保留失败记录。

## E. 交付前

- [ ] 确认论文只引用有对应证据层的数字。
- [ ] 重跑 `make v15-finalize`、发布包验证和 SHA-256 检查。
- [ ] 完成投稿平台作者、冲突、配额、披露和无并行投稿人工门槛。
- [ ] 更新本目录中文状态文档、英文审计文件和交接清单中的下一步。
