# V7 Evidence Ledger (2026-08-25)

This ledger records the first V7 implementation slice without promoting pilot
results to confirmatory claims.

## Frozen baseline

V9 is archived at `archive/submission_v9` from commit
`7428d04a1cf63c08cb6ca231eb05b5ee2880ba8c`. Its 359-file hash manifest,
9-page PDF, 14-page total package, V6 analytic artifact, E3 ceiling null,
strategic fixture, and finance 0/7 primary and 0/5 holdout negative boundary
are unchanged.

## Theory and operator shift

The new `operator_shift` module implements the discrete
`IF <= sqrt(E_P[ell^2]) sqrt(1+chi2(Q_A||P))` bound, effective sample size, and
operator-weighted absolute/sign losses. Registered E2 uses 63 paired rows and
the frozen temperature law
`Q_beta(tau) proportional exp(beta * centered_delta_proxy(tau))`.

The beta sweep `[0.25, 0.5, 1, 2, 4]` reports chi-square divergence increasing
from `0.0572` to `1.4466` and absolute IF increasing from `10.8833` to
`15.5595`; every row satisfies the analytic bound. Its state is
`HYPOTHESIS_SUPPORTED`, but this is controlled mechanism evidence, not an
external causal result.

## External adaptive development

The pinned candidate source is Farama MPE2 `simple_adversary_v3` and
`simple_spread_v3`, accessed through PettingZoo 1.27.0 and MPE2 1.1.0. The
first E3b confirmatory attempt with a discrete observer score is frozen as
`DESIGN_INVALID`; the redesigned short-horizon direct-replay proxy passes all
five construct gates. Its powered confirmatory comparison has state
`HYPOTHESIS_NOT_SUPPORTED`: the PIVOT-VOI minus Proxy Only CTI difference is
`-3.8672173` over 772 paired trajectories. E4b uses trajectory-disjoint
held-out rows and its powered result is `HYPOTHESIS_NOT_SUPPORTED`. E7b's
independent best-response opponent family is powered with 140 valid clusters
(required 135) and has state `HYPOTHESIS_SUPPORTED`; the cluster-level
strategic effect is `-0.5884429` against the frozen `-0.5` threshold. The claim
is restricted to that held-out opponent family.

## Method and integrity

PIVOT-VOI now has a Bayesian linear correction posterior, Monte-Carlo EVSI per
cost, posterior-confidence/EVSI stopping, auditable query scores, and the
explicit sub-Gaussian sample bound. PIVOT-H remains a baseline. ImprovementBench
V7 is rebuilt only after the final valid experiment outputs; connected
components over trajectory/environment/operator/opponent/response groups keep
each source split leakage-safe rather than leaking confirmatory transitions
into test.

The current state is therefore a scientifically interpretable final V7 result:
the theory and controlled operator-shift gate pass; powered E3b/E4b nulls
bound the generic external improvement claim, while powered E7b evidence
supports strategic reversal for the registered held-out opponent family.
